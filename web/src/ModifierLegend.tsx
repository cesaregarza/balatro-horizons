export function ModifierLegend() {
  return (
    <details className="modifier-legend">
      <summary>Card modifier key</summary>
      <p>
        F = foil · H = holographic · P = polychrome · N = negative · ∞ = eternal
        · X = perishable (number = rounds left) · R = rental · D = debuffed
      </p>
    </details>
  );
}
