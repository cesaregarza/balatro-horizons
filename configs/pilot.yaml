# Shared defaults (including Red/Gold and disabled paid execution):
# src/balatro_horizons/config.py. This preset only supplies the development panel.
sampling:
  split: development
  seed_manifest: private/dev.json
  seed_count: 20
  model_replicates_per_seed: 2
  max_infrastructure_replacements: 1
